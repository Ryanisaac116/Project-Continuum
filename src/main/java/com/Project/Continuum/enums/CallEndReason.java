package com.Project.Continuum.enums;

public enum CallEndReason {
    COMPLETED,
    REJECTED,
    CANCELLED,
    TIMEOUT,
    EXCHANGE_ENDED,
    STALE_TIMEOUT,
    FORCE_CLEARED,
    DISCONNECTED,
    ERROR
}
