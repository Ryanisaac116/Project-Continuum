package com.Project.Continuum.enums;

/**
 * CallType - Distinguishes between friend calls and exchange calls.
 * 
 * FRIEND: Casual call between friends (anytime)
 * EXCHANGE: Call within an active exchange session (structured)
 */
public enum CallType {
    FRIEND,
    EXCHANGE
}
