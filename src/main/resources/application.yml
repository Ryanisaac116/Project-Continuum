spring:
  application:
    name: Continuum

  frontend-url: ${FRONTEND_URL:http://localhost:5173}

  profiles:
    group:
      prod-verify: prod

  jackson:
    serialization:
      write-dates-as-timestamps: false
    time-zone: UTC

  jpa:
    show-sql: true
    properties:
      hibernate:
        jdbc:
          time_zone: UTC
