spring:
  config:
    activate:
      on-profile: dev

  datasource:
    url: jdbc:postgresql://localhost:5432/continuum
    username: postgres
    password: pg/1234
    driver-class-name: org.postgresql.Driver

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: true

# ===============================
# JWT CONFIG (DEV ONLY)
# ===============================
jwt:
  secret: continuum-dev-secret-key-change-in-prod
  expiration-ms: 86400000

# ===============================
# WEB PUSH (VAPID) CONFIG (DEV)
# ===============================
push:
  vapid:
    public-key: BEl62iUYgUivxIkv69yViEuiBIa-Ib9-SkvMeAtA3LFgDzkrxZJjSgSnfckjBJuBkr3qBUYIHBQFLXYp5Nksh8U
    private-key: UUxI4O8-FbRouADVXc-hK-l-dR0OYDy3fOY7EHmWnqs
    subject: mailto:dev@continuum.local

    hibernate:
      ddl-auto: update
  jackson:
    serialization:
      write-dates-as-timestamps: false

# ===============================
# GOOGLE OAUTH CONFIG (DEV)
# ===============================
google:
  client:
    id: ${GOOGLE_CLIENT_ID:placeholder-id}
    secret: ${GOOGLE_CLIENT_SECRET:placeholder-secret}
  redirect:
    uri: ${GOOGLE_REDIRECT_URI:http://localhost:8080/api/auth/google/callback}

